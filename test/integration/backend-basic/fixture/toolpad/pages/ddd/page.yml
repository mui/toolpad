apiVersion: v1
kind: page
spec:
  id: 4B1b643
  title: ddd
  display: shell
  queries:
    - name: query
      query:
        kind: rest
        searchParams:
          - name: sd
            value:
              $$jsExpression: |
                `dd${parameters.sd}`
        headers:
          - name: dd
            value:
              $$jsExpression: |
                `cc:${parameters.sd}`
        method: GET
      parameters:
        - name: sd
          value: Some bar secret
