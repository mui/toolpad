apiVersion: v1
kind: page
spec:
  id: odA4VjX
  title: page
  display: shell
  content:
    - component: PageRow
      name: pageRow
      children:
        - component: TextField
          name: textField
          props:
            defaultValue:
              $$jsExpression: |
                String(Date.now())
