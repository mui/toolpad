{
  "name": "create-toolpad-app-test",
  "version": "0.0.13",
  "keywords": [
    "react",
    "toolpad",
    "mui"
  ],
  "description": "Create Toolpad apps with one command",
  "repository": {
    "type": "git",
    "url": "https://github.com/mui/mui-toolpad",
    "directory": "packages/create-toolpad-app"
  },
  "author": "Toolpad Team",
  "license": "MIT",
  "bin": {
    "create-toolpad-app-test": "./dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "dev13": "ncc build ./index.ts -w -o dist/",
    "prerelease": "rimraf ./dist/",
    "release": "ncc build ./index.ts -o ./dist/ --minify --no-cache --no-source-map-register",
    "build": "yarn release",
    "releaseWithPublish": "yarn release && yarn publish"
  },
  "engines": {
    "node": ">=14.6.0"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/inquirer": "^9.0.3",
    "@vercel/ncc": "^0.36.1",
    "chalk": "^5.2.0",
    "execa": "^7.0.0"
  }
}
