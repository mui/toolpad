{
  "name": "create-toolpad-app",
  "version": "0.0.42-alpha.5",
  "keywords": [
    "react",
    "toolpad",
    "mui"
  ],
  "description": "Create Toolpad apps with one command",
  "repository": {
    "type": "git",
    "url": "https://github.com/mui/mui-toolpad",
    "directory": "packages/create-toolpad-app"
  },
  "author": "Toolpad Team",
  "license": "MIT",
  "bin": {
    "create-toolpad-app-test": "./dist/index.js"
  },
  "files": [
    "dist"
  ],
  "type": "module",
  "scripts": {
    "dev13": "tsc -w -p ./tsconfig.json --outDir ./dist/",
    "prerelease": "rimraf ./dist/",
    "release": "tsc -p ./tsconfig.json -outDir ./dist/",
    "build": "yarn release"
  },
  "engines": {
    "node": ">=14.6.0"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/inquirer": "^9.0.3",
    "chalk": "^5.2.0",
    "execa": "^7.0.0"
  }
}
