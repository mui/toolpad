apiVersion: v1
kind: page
spec:
  id: mIM1RAc
  title: page
  display: shell
  content:
    - component: PageRow
      name: pageRow
      children:
        - component: Checkbox
          name: checkbox
          props:
            defaultValue: false
