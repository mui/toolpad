# yaml-language-server: $schema=https://raw.githubusercontent.com/mui/mui-toolpad/v0.1.45/docs/schemas/v1/definitions.json#properties/Page

apiVersion: v1
kind: page
spec:
  title: adminpage
  display: shell
  authorization:
    allowAll: false
    allowedRoles:
      - admin
  displayName: Admin Page
  content:
    - component: Text
      name: text
      props:
        value: I just want to tell all the admins out there to never stop
          administrating.
