apiVersion: v1
kind: page
spec:
  id: 5w03xpv
  title: list
  content:
    - component: PageRow
      name: pageRow
      children:
        - component: List
          name: list
          props:
            itemCount: 2
            renderItem:
              $$template:
                - component: PageRow
                  name: pageRow1
                  children:
                    - component: TextField
                      name: textField
                      props:
                        label:
                          $$jsExpression: |
                            `textField${i}`
                    - component: Text
                      name: text
                      props:
                        value:
                          $$jsExpression: |
                            textField.value
